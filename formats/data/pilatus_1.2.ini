[_top_]
detector = "%{{# Detector: (.+?),}}"
pixel_size = "# Pixel_size %6e m x %6e m"
exposure_time = "%{{# Exposure_time (-?\d*\.\d*) s}f}"
saturated_value = "# Count_cutoff %7d counts"
wavelength = "%{{# Wavelength (-?\d*\.\d*) A}f}"
distance = "%{{# Detector_distance (-?\d*\.\d*) m}f}"
beam_center = "# Beam_xy (%7f, %7f) pixels"
start_angle =  "%{{# Start_angle (-?\d*\.\d*) deg.}f}"
delta_angle = "%{{# Angle_increment (-?\d*\.\d*) deg.}f}"
two_theta = "%{{# Detector_2theta (-?\d*\.\d*) deg.}f}"
sensor_thickness = "# Silicon sensor, thickness %8f m"